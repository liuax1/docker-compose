/*
package com.example.demo.es.controller;

import com.example.demo.es.entity.Product;
import com.example.demo.es.jpa.ProductDao;


import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
class ESControllerTest {
    @Autowired
    ProductDao productDao;
    @Test
    void save() {
        Product product = new Product();
        product.setCategory("国产手机");
        product.setId(1L);
        product.setTitle("小米手机");
        product.setPrice(2999.00);
        productDao.save(product);
    }
}*/
